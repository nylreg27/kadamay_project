# apps/contribution_type/urls.py
from django.urls import path

app_name = 'contribution_type'  # Always good to define the app_name

urlpatterns = [
    # You can add your paths for contribution types here later
    # Example:
    # path('list/', views.contribution_type_list, name='list'),
    # path('create/', views.contribution_type_create, name='create'),
]
